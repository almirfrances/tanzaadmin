<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Admin Route Prefix
    |--------------------------------------------------------------------------
    |
    | This value is used to set the prefix for the admin routes. It allows
    | you to easily change the prefix for all admin routes in your application.
    |
    */

    'route_prefix' => env('ADMIN_ROUTE_PREFIX', 'admin'), // Default to 'admin' if not set in .env

];
